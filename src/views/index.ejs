<%- include('includes/head') %>
<body>

  <section class="container">
    <h1>Lorem normal nen<BR>
      <%= umaVariavelLocal %> <!--de middleware.js-->
      <!--
      <% if( titulo !== 'undefined') { %>
        <%= titulo %> -- chave titulo do arq. homeController.js --
      <% } else { %>     
        O título não foi informado
      <% } %>
      <% if(numeros) {%> 
        <%= numeros %><br> -- chave numeros homeController.js --
        <% numeros.forEach(num => { %> -- com forEach ou for in--
          <%= num %><br>
        <% }) %>
      <% } %> -->
    </h1>


    <form action="/" method="post">
      <label>Cliente</label>
      <input type="text" name="cliente">
      <button>Submit</button>
    </form>
  </section>

  <%- include('includes/footer') %>
